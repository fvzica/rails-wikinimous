<div class="container">
  <div class="row justify-content-center">
     <div class="col-4">
        <h1><%= @article.title %></h1>
        <p><%= @article.content %></p>
      </div>
  </div>
</div>
    <p><%= link_to edit_article_path(@article) do %><i class="fa-solid fa-pen"></i><% end %></p>
    <p><%= link_to article_path(@article), method: :delete, data: {confirm: "Are you sure?"} do %>
<i class="fa-solid fa-trash"></i>
<% end %></p>
